# This is a basic workflow to help you get started with Actions

name: CI/CD dev

on:
  push:
    branches: [ dev ]

  workflow_dispatch:

jobs:
  test:
    name: Test
    uses: ./.github/workflows/test.yml
